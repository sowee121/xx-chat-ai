---
description: UI 样式约定：优先 shadcn 默认样式，长 class 抽离到 *.styles.ts
globs: apps/web/src/**/*.tsx
alwaysApply: false
---

# UI 样式约定

编写 / 修改前端 UI 组件时遵循：

1. 优先使用 shadcn 组件的默认样式与内置变体（`variant` / `size`），非必要不覆盖原语（`Button`/`Input` 等）的视觉样式。
2. 大圆角由全局 `--radius` 驱动（当前 `1rem`）；不要随手写 `rounded-[…]`，改圆角只调 `index.css` 的 `--radius`。
3. 组件 JSX 内不堆长 class：抽到同级 `ComponentName.styles.ts`，`export const styles = { … } as const`，JSX 用 `className={styles.x}`。
4. 条件样式：`cn(styles.base, cond && styles.variant)`。
5. 覆盖 shadcn 原语时用 utility 字符串透传 `className`（依赖 tailwind-merge）；不要用 `@apply` 覆盖原语。
6. 不引入额外 UI 库或新样式体系（Tailwind + shadcn 之外）。
7. 不做 plan 明确排除的视觉：gradient 光斑、GlassPanel、自定义 `@theme` 色板覆盖。
