---
description: 项目改动后检查并同步更新 docs/plan.md 全栈方案文档
alwaysApply: true
---

# Plan 文档同步约定

本仓库方案文档：**`docs/plan.md`**（项目内主文档）。Cursor plan 副本：`.cursor/plans/ai对话demo最终方案_a13dc516.plan.md`（有实质更新时一并同步，去掉 Cursor frontmatter 差异即可）。

## 何时必须检查

完成以下任一改动后，**主动判断** plan 是否需要更新，并在本次任务收尾说明「已更新 plan」或「无需更新 plan」：

- 技术栈 / 依赖 / 构建方式变更
- 新增或删除 API、SSE 事件、配置项
- 目录结构、模块职责、核心数据流变化
- UI 布局、交互规范、样式约定（含 Streamdown 覆盖规则）
- Provider / 模型 / 存储 / 鉴权方案变更
- 实现阶段完成度、范围（已做/不做）变化
- 本地启动、配置方式、环境变量约定变更
- 已知问题与修复记录（值得后人复用的）

## 何时通常不必更新

- 纯 bug 修复且与文档描述行为一致
- 变量重命名、内部重构不改变对外行为
- 样式微调且未改变 plan 已写明的规范

## 更新原则

1. **先读** `docs/plan.md`，只改与本次 diff 相关的章节，避免过度精简或重复堆砌。
2. **保持** 与代码、README、`.env.example` / `config.local.example.json` 一致。
3. **不要** 在 plan 中写入真实 API Key 或密钥。
4. 用户明确要求「不要动 plan」时，以用户为准。

## 执行习惯

每次实现类任务结束前，用一句话交代 plan 同步结论，例如：

-「已同步更新 `docs/plan.md` 第 6、7 节（新增 models API）」
-「本次仅为样式微调，plan 无需更新」
