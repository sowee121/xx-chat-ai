---
description: XX Chat AI 后端约定（Fastify / SSE / Provider / SQLite）
globs: apps/server/**
alwaysApply: false
---

# 后端规则

## 技术栈

Fastify 5 · TypeScript 6 · openai SDK · better-sqlite3

## 路由

- 聊天：`POST /api/chat` — SSE，`reply.raw` 流式写出；`close` 时持久化已生成**正文**。
- 历史：`/api/history` CRUD，实现见 `store/sqlite.ts`。
- Provider：`GET /api/providers`、`GET /api/providers/openai/models`（10min 缓存）。

## Provider 架构

```
providers/
  mock.ts    关键词意图，产出 { type: 'text', content }
  openai.ts  openai SDK 流式 + models.list + 推理归一化
  config.ts  可用性探测；label 为「OpenAI」
  index.ts   按 provider 分发
lib/
  thinkingParser.ts   流式标签解析 → reasoning | text
  reasoningDelta.ts   delta 多字段推理提取
```

- `ChatStream` 产出 `StreamChunk { type: 'reasoning' | 'text', content: string }`，勿改回裸 `string`。
- `openai.ts`：先 `extractReasoningFromDelta`，再对 `content` 走 `ThinkingStreamParser`；`finally` 中 `flush()`。
- 历史 `appendMessage` 只存 `text`；落库前 `stripThinkingTags()`。
- 系统提示：Mermaid 数值图用 `xychart-beta`，禁止 `barChart`。

## 配置

- 读取：`config/local.ts` → `config.local.json`；`OPENAI_*`、`XX_PORT` / `XX_DB_PATH` / `XX_DEFAULT_PROVIDER` 环境变量覆盖。
- 模板：`config.local.example.json`（进仓库）；真实 `config.local.json` gitignore。
- 默认 DB：`data/chat.db`（WAL）；可用 `XX_DB_PATH` 覆盖。

## SSE 事件格式（勿破坏兼容）

```ts
// meta → delta (多次) → done | error
event: meta   data: { sessionCode, title }
event: delta  data: { type: 'reasoning' | 'text', content: string }
event: done   data: { sessionCode, finishReason }
event: error  data: { message: string }
```

- 缺省 `type` 时前端按 `text` 处理（向后兼容）。
- 多轮 `messages` 仅含 `content` 正文，不含 `reasoning`。

## 注意

- 不使用 `dotenv`；生产靠平台 Secret 注入环境变量。
- `better-sqlite3` 为 native 依赖，改 Node 版本后可能需要 `pnpm rebuild`。
