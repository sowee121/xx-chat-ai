---
description: XX Chat AI 项目总览、硬性约束与开发习惯
alwaysApply: true
---

# XX Chat AI — 项目规则

**方案文档**：`docs/plan.md`（改动后按 `plan-sync` 规则判断是否同步）。

## 定位与约束

- **全栈 AI 聊天**：React 前端 + Fastify 后端，SSE 流式对话。
- **硬性约束**：前端必须用 **React + `@microsoft/fetch-event-source`**，禁止换 SSE 客户端库。
- **Provider 命名**：界面与 API 展示用 **OpenAI**；文档中说明其为 OpenAI API 兼容协议（edgefn、DeepSeek 等）。
- **范围外不做**：Typewriter、多模态上传、代码块全屏（Streamdown 不支持）、tool/citation 结构化片段。

## 仓库结构

```
apps/web/     Vite 8 + React 19 + shadcn + Zustand + Streamdown
apps/server/  Fastify 5 + providers(mock/openai) + SQLite 历史
docs/plan.md  全栈方案（单一事实来源）
```

- Monorepo：`pnpm dev` 同时起 web `:5173`、server `:3001`。
- 配置：`apps/server/config.local.json`（gitignore）；环境变量 `OPENAI_*`、`XX_*` 优先；**不用 dotenv**。
- 密钥不进 Git、不进 plan、不进提交。

## 编码原则

1. **最小改动**：只改任务相关文件，不顺手重构。
2. **沿用现有模式**：新 API 仿 `routes/`；新 Provider 仿 `providers/`；前端状态进 `chatStore`。
3. **TypeScript**：保持严格类型；共享类型放各 app 内 `types/` 或就近定义。
4. **中文用户文案**：错误提示、底栏说明等面向用户的中文保持一致、可读。
5. **不引入**：额外 UI 库、自定义色板覆盖、gradient 光斑等非 plan 约定视觉。
6. **注释**：
   - 文件头、导出函数/常量（及类型字段）：`/** … */`
   - 函数体内某一行逻辑：优先 `//`
   - **JSX 内不写注释**，以免干扰阅读。

## 关键文件速查

| 区域 | 路径 |
| --- | --- |
| 聊天状态 / SSE | `apps/web/src/stores/chatStore.ts` |
| 推理块 UI | `apps/web/src/components/chat/ReasoningBlock.tsx` |
| Mermaid 修复 | `apps/web/src/lib/mermaidPlugin.ts`、`sanitizeMermaid.ts` |
| 根布局 | `apps/web/src/App.tsx`（须包 `TooltipProvider`） |
| Streamdown 覆盖 | `apps/web/src/index.css` |
| 聊天 SSE | `apps/server/src/routes/chat.ts` |
| OpenAI + 推理 | `apps/server/src/providers/openai.ts` |
| 思考解析 | `apps/server/src/lib/thinkingParser.ts`、`reasoningDelta.ts` |
| 本地配置 | `apps/server/src/config/local.ts` |

## 任务收尾

- 实现类改动：一句话说明 plan 是否已同步。
- 未要求时不主动 `git commit` / 推远程。
